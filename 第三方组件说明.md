# 第三方组件说明

本项目的源码和观众运行包包含或使用以下第三方组件。发布 GitHub 仓库和 Release 资产时，请保留这些说明及组件自带的许可文件。

## 源码仓库内

- FunASR：源码位于 `apps/funasr`，许可文件为 `apps/funasr/LICENSE` 和 `apps/funasr/MODEL_LICENSE`。
- SenseVoice：适配代码位于 `apps/sensevoice`，许可文件为 `apps/sensevoice/LICENSE`。
- Astro、React、TypeScript、Vitest 和相关前端依赖：通过 `pnpm-lock.yaml` 安装，具体许可随依赖包提供。

## 观众运行包内

- yt-dlp：用于网址视频导入。项目地址：https://github.com/yt-dlp/yt-dlp
- FFmpeg：用于音视频转换和抽取。项目地址：https://ffmpeg.org/
- Deno：用于部分 JavaScript 运行环境。项目地址：https://deno.com/
- FunASR、SenseVoice 和相关 Python 依赖：随包放在 `runtime/python`，Python 依赖的许可文件位于对应的 `*.dist-info` 目录。
- SenseVoice 模型及其依赖模型：位于模型包的 `models/sensevoice`，使用时请遵守模型来源方的模型许可。

运行包只在本机 `127.0.0.1` 提供服务。网址导入时会访问用户主动提交的网址，并可能按用户选择读取本机浏览器登录态；Cookie 不保存到包内。
